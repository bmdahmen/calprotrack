# Git workflow

This is a personal, single-user project — there's no one else to review a PR.
Feature work should end with a direct push to `main`, not a dangling PR branch.
Skip opening a pull request unless explicitly asked for one.
